import React, { Component } from 'react';
import { observer } from 'mobx-react';

@observer
class Main extends Component {
  render() {
    return (
      <div>
        <h1>Main</h1>
      </div>
    );
  }
};

export default Main;

